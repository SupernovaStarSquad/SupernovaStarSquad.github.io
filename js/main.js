$(document).on('ready', function() {
  $(".regular").slick({
    dots: true,
    infinite: true,
    speed: 500,
    fade: true,
    cssEase: 'linear',
    autoplay: true,
autoplaySpeed: 2500,
});
});
